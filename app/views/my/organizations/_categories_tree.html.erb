<% categories.each do |category| %>
  <div class='category_item'>
    <span><%= link_to category.title, '#', '@click': "setCategory(#{category.id})" %></span>
    <ul>
      <%= render partial: 'categories_tree', locals: { categories: category.children } %>
    </ul>
  </div>
<% end %>
